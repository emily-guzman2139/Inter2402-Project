<!-- views/enrollments/listEnrollments.ejs -->

<!--<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Enrollments</title>
    <style>
        /* Style for the table */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        /* Style for buttons */
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .button:hover {
            background-color: #0056b3;
        }

        /* Style for error message */
        .error-message {
            color: red;
            font-style: italic;
            margin-bottom: 10px;
        }
    </style>
</head>

<body>
    <h1>List of Enrollments</h1>

    <% if (enrollments.length > 0) { %>
    <table>
        <thead>
            <tr>
                <th>Student Name</th>
                <th>Course Name</th>
                <th>Faculty Name</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            <% enrollments.forEach(enrollment => { %>
            <tr>
                <td><%= enrollment.student_name %></td>
                <td><%= enrollment.course_name %></td>
                <td><%= enrollment.faculty_name %></td>
                <td>
                    <a href="/enrollments/edit/<%= enrollment.id %>" class="button">Edit</a>
                    <a href="/enrollments/delete/<%= enrollment.id %>" class="button">Delete</a>
                </td>
            </tr>
            <% }); %>
        </tbody>
    </table>
    <% } else { %>
    <p>No enrollments found.</p>
    <% } %>

    <a href="/enrollments/enrollStudent" class="button">Enroll Student</a>

    
</body>

</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>College Management System - List of Enrollments</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">List of Enrollments</h1>
        <a class="btn btn-primary mt-3" href="/enrollments/enrollStudent">Enroll Student</a>
        <table class="table table-bordered table-striped mt-3">
            <thead>
                <tr>
                    <th>Enrollment ID</th>
                    <th>Student Name</th>
                    <th>Course Name</th>
                    <th>Faculty</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                <% enrollments.forEach(function(enrollment) { %>
                    <tr>
                        <td><%= enrollment.enrollment_id %></td>
                        <td><%= enrollment.student_name %></td>
                        <td><%= enrollment.course_name %></td>
                        <td><%= enrollment.faculty_name %></td>
                        <td>
                            <a class="btn btn-danger" href="/enrollments/removeFaculty/<%= enrollment.enrollment_id %>">Remove Faculty</a>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>

